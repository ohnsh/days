---
import { Icon, Badge } from '@astrojs/starlight/components'
import Commit from './Commit.astro'

const { repo, commits } = Astro.props
const { html_url, full_name, name } = repo
---

<details>
  <summary>{name} commits <span class="badge">{commits.length}</span></summary>
  <a class="button badge not-content" target="_blank" href={html_url}>
    <Icon name="github" />
    {full_name}
  </a>
  <ul>
    {commits.map((commit) => <Commit {commit} />)}
  </ul>
</details>

<style>
  details > :not(summary) {
    font-size: var(--sl-text-sm);
    margin-inline-start: 2em;
  }
  summary > span {
    font-size: var(--sl-text-xs);
    margin-inline-start: .5em;
  }
  .badge {
    padding: 0.07em 0.3em .1em;
    border: solid 1px var(--sl-color-gray-4);
    color: var(--sl-color-gray-3);
    border-radius: 0.4em;
    font-weight: normal;
  }
  ul {
    list-style: none;
    padding-inline-start: 5px;
  }
  a.button {
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    text-decoration: none;
    padding: 0.1em 0.5em;

    &:hover {
      color: var(--sl-color-gray-2);
      border-color: var(--sl-color-gray-2);
    }
  }
</style>
