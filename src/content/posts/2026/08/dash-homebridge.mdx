---
title: Using a fake HomeKit switch to automate capturing sensor data
date: 2026-08-25
---

import actionHome from 'assets/action-home.png'
import configHomebridge from 'assets/config-homebridge.png'
import dummySwitchHome from 'assets/dummy-switch-home.png'
import logsHomebridge from 'assets/logs-homebridge.png'

import Slideshow from '@/components/Slideshow.astro'
import { Image } from 'astro:assets'

export const slides = [
    { image: actionHome, alt: '', caption: 'In Apple Home, an action runs whenever the Homebridge virtual (dummy) switch is "pressed" (every 15 minutes). The action sends a JSON payload to dashd (the backend server/database) consisting of HomePod sensor readings and a timestamp.' },
    { image: configHomebridge, alt: '', caption: 'In Homebridge, the virtual switch is set to trigger every 15 minutes, and also to create a local webhook (simple HTTP endpoint) at which it can be triggered on-demand.' },
    { image: dummySwitchHome, alt: 'Apple Home provides a settings panel for the Homebridge virtual switch, including actions that can be run on single press, double press, and long press.', caption: `Main settings panel for virtual switch in Apple Home.` },
]

<Slideshow slides={slides} />

[Homebridge](https://homebridge.io/) is an open-source, virtual HomeKit bridge that runs on inexpensive hardware (I'm using a Raspberry Pi). It allows non-HomeKit devices to be added to and controlled by Apple Home. [homebridge-dummy](https://github.com/mpatfield/homebridge-dummy) is a Homebridge plugin providing virtual switches and other fake "devices" that can trigger or extend HomeKit automations.

As a demo, I'm using this setup to push sensor readings from my HomePods to a database that powers live charts on [dash.ohn.sh](https://dash.ohn.sh). Readings are taken every 15 minutes in response to the virtual switch being "pressed." The automation takes the form of a [Shortcut](https://apps.apple.com/us/app/shortcuts/id915249334) that uses the built-in "Get Contents of URL" action to send the data to my [custom backend](https://github.com/ohnsh/dash/blob/main/dashd/src/index.ts).
